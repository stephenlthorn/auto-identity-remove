# Cross-model review - 1994125

| | |
|---|---|
| reviewer family | OpenAI (non-Anthropic) |
| tool | codex CLI |
| model | codex default |
| base | origin/main |
| head | 1994125 |
| files reviewed | 4 |
| findings | P1: 1, P2: 1, P3: 0 |

## Summary

Reviewed all four requested files in full. There is 1 P1 privacy leak and 1 P2 false-clean review failure; no other P1 found.

## Files reviewed

- `package.json`
- `scripts/cross-model-preflight.sh`
- `scripts/cross-model-review.sh`
- `test/cross-model-preflight.test.js`

## Findings

Four rounds were run against this change, each over the fixes from the round
before. Seven findings in total; six fixed, one declined. They are recorded
together because the sequence is the point: the probe that exists to stop a
review passing silently shipped, twice, with a hole that would have let a review
pass silently.

| # | Round | Sev | Location | Finding | Disposition |
|---|-------|-----|----------|---------|-------------|
| 1 | 1 | P1 | `cross-model-review.sh` checkout builder | Every untracked, non-ignored file was copied into the upload. "Scrubbed by construction" only covered paths someone had thought to gitignore. | **fixed** - untracked files now pass `may_upload_untracked()`; verified with a scratch file containing fake PII, which the run names and refuses |
| 2 | 1 | P2 | `cross-model-review.sh` preflight wiring | `if ! bash preflight; then exit $?; fi` exits **0**: inside the then-block of a negation, `$?` is the negation's status. A dead reviewer made the review exit 0, which a git hook reads as a pass. | **fixed** - `... \|\| exit $?`; regression test asserts the probe's code 3 reaches the caller |
| 3 | 2 | P1 | `cross-model-preflight.sh` sentinel check | `codex exec` echoes the prompt, so grepping for a sentinel the prompt contains passes whenever codex printed anything - including the 0.137.0 breakage the probe was written for. | **fixed** - challenge whose answer is absent from the prompt (`PROBEOK-4`), and exit status must also be 0 |
| 4 | 2 | P2 | `cross-model-review.sh` untracked JSON | `*.json` was uploadable, so an untracked `config.backup.json` or `addresses.json` would be sent. | **fixed** - untracked JSON is refused unless it is a manifest |
| 5 | 3 | P3 | `cross-model-preflight.sh` auth check | `grep -qi "logged in"` matches **"Not logged in"**. The check only worked because the real CLI also exits non-zero when logged out. | **fixed** - explicit negative match plus a required zero exit; test drives a stub that reports logged-out on a zero exit |
| 6 | 3 | P2 | `cross-model-review.sh` renderer | A reviewer that died part-way could leave a non-empty findings file, which was rendered into a report. | **fixed** - non-zero `CODEX_STATUS` fails before the file is looked at. Honest caveat: I could not construct a case that actually exited 0, so this is defensive rather than a demonstrated leak |
| 7 | 4 | P2 | `cross-model-review.sh` target selection | `git diff ... \|\| true` turned a git failure into an empty change set, printing "nothing to do" and exiting 0. | **fixed** - git failure exits 6. Verified by reading and `bash -n`; not unit-tested, because forcing `git diff` to fail without breaking the rest of the script means stubbing git itself |

### Declined

| # | Round | Sev | Finding | Why not |
|---|-------|-----|---------|---------|
| 8 | 1, 3, 4 | P1 | "Build the checkout from an explicit allowlist; do not upload arbitrary **tracked** files." | Tracked files are the public repository. Sending them to a reviewer is what a review is, and an allowlist would mean the reviewer could not see the context around the code it is judging. The concrete risks named inside this finding are already handled: `config.json`, `config.json.enc`, `state.json*`, `inbox/`, `.claude/` and `.env` are gitignored and additionally refused at exit 5, and tracked **symlinks** are now skipped so `cp` cannot follow one out of the repo. Raised in three separate rounds with the same reasoning each time and declined on the same grounds; recorded here so the next round does not relitigate it silently. |

### A note on the rounds

Rounds 2, 3 and 4 found defects in code written to fix rounds 1, 2 and 3. Every
one of them was the same shape: a failure path that reports success. That is
worth stating plainly rather than presenting seven findings as one clean pass.

Stopping here is a judgement call, not a claim that the file is now correct. The
severity is falling (round 4 is a `|| true` on a command that does not fail in
practice) and the one P1 still outstanding is declined on reasoning rather than
unfixed through oversight. The next person to touch this file should run
`npm run review:cross-model` again and expect finding 8 to come back.
